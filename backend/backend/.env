#No puede haber ningún espacio entre variables y valores
SECRET_KEY='!r0-fg^!p_e6#i_i@d1#z+@3he+tf3+g)pp%_4zta=_(=vk(2o'
DEBUG=True
ALLOWED_HOSTS_DEV='*'
ALLOWED_HOSTS_DEPLOY='http://PEBquiz_uib.com', 'www.PEBquiz_uib.com'

#el puerto de react
CORS_ALLOWED_ORIGINS_DEV='http://localhost:3000'
CORS_ALLOWED_ORIGINS_DEPLOY='https://PEBquiz_uib.com', 'www.PEBquiz_uib.com'

CSRF_TRUSTED_ORIGINS_DEV='http://localhost:3000'
CSRF_TRUSTED_ORIGINS_DEPLOY='https://PEBquiz_uib.com', 'www.PEBquiz_uib.com'

ENGINE=django.db.backends.postgresql
DATABASE_URL= ' '

DB_NAME_DEV=quiz2
DB_USER_DEV=postgres
DB_PASSWORD_DEV=my-secret
DB_HOST_DEV=localhost
DB_PORT_DEV=5432

# # True available only HTTPS connections, is to prevent sniffer a cookie session cause if is True only permits HTTPS conection.
# SESSION_COOKIE_SECURE_DEV=False
# #  is set to True, client-side JavaScript will not be able to access the session cookie.
# SESSION_COOKIE_HTTPONLY_DEV=False
# # if is True all non-HTTPS are redirected to HTTPS excepts SECURE_REDIRECT_EXMEPT list.
# SECURE_SSL_REDIRECT_DEV=False
# # Return True if request is secure between Django and the Proxy if is a HTTPS conection, isn't it returns False. 
# # To protect csrf
# SECURE_PROXY_SSL_HEADER_DEV=None
# # Use secure cookie marks the cookie if is set to True and sent only with HTTPS connections.
# CSRF_COOKIE_SECURE_DEV=False

# SESSION_COOKIE_SECURE_DEPLOY=True
# SESSION_COOKIE_HTTPONLY_DEPLOY=True
# SECURE_SSL_REDIRECT_DEPLOY=True
# SECURE_PROXY_SSL_HEADER_DEPLOY= None #('HTTP_X_FORWARDED_PROTO', 'https')
# CSRF_COOKIE_SECURE_DEPLOY=True